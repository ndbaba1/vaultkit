# Database Configuration
POSTGRES_USER=vaultkit
POSTGRES_PASSWORD=change_this_password
POSTGRES_DB=vaultkit_development
DATABASE_URL=postgres://vaultkit:change_this_password@postgres:5432/vaultkit_development

# Application Configuration
RAILS_ENV=development

# FUNL URL - use Docker service name
FUNL_URL=http://funl-runtime:8080

# Key Paths - inside container filesystem
VKIT_PRIVATE_KEY=/secrets/keys/vkit_priv.pem
VKIT_PUBLIC_KEY=/secrets/keys/vkit_pub.pem